{% extends 'base.html.twig' %}

{% block title %}Picture{% endblock %}

{% block body %}
    <h1>Picture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picture.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ picture.description }}</td>
            </tr>
            <tr>
                <th>Day_of_taking</th>
                <td>{{ picture.dayOfTaking }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ picture.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_country_index') }}">back to list</a>

    <a href="{{ path('admin_country_edit', {'id': picture.id}) }}">edit</a>

    {{ include('Admin/country/_delete_form.html.twig') }}
{% endblock %}
