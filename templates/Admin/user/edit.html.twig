{% extends "base.html.twig" %}

{% block body %}
    <div class="col-md-10 m-auto pt-5 pb-5">
        <h2 class="text-center">Éditer le l'utilisateur</h2>
        <div class="container">
            <a href="{{ path('admin_user_index') }}" class="cross"></a>
            <h1>Edition de la news "{{user.email }}"</h1>
            {{ include('Admin/user/_form.html.twig', {'button_label': 'Update'}) }}
            <br>
            <div class="text-right"> {{ include('Admin/user/_delete_form.html.twig') }}</div>
        </div>
    </div>
{% endblock %}
