{% extends "base.html.twig" %}

{% block body %}
    <div class="col-md-10 m-auto pt-5 pb-5">
        <h2 class="text-center">Éditer la news</h2>
        <div class="container">
            <a href="{{ path('admin_news_index') }}" class="cross"></a>
            <h1>Edition de la news "{{ news.title }}"</h1>
            {{ include('admin/news/_form.html.twig', {'button_label': 'Update'}) }}
            <br>
            <div class="text-right"> {{ include('admin/news/_delete_form.html.twig') }}</div>
        </div>
    </div>
{% endblock %}
