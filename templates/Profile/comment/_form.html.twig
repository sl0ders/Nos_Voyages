{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn button-primary btn-sm" name="id" value="{{ picture.id }}">{{ button_label|default('Déposer le commentaire') }}</button>
{{ form_end(form) }}
