{{ form_start(form, {'attr' : {'class' : 'm-auto text-center p-5 col-md-10 h4'}}) }}
    {{ form_row(form.countries, {'attr' : {'class' : 'd-flex justify-content-around flex-wrap'}}) }}
    {{ form_row(form.cities) }}
<button class="btn btn-sm btn-primary text-center p-1" type="submit">Rechercher</button>
{{ form_end(form) }}
